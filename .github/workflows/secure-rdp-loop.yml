name: Secure RDP via Tailscale (Automated Loop)

on:
  workflow_dispatch:
    inputs:
      gh_api_token:
        description: "🔒 ToolboxLap | ENTER YOUR GITHUB PAT HERE (Starts with ghp_...)"
        required: true
        default: ""
      loops:
        description: "🔄 System Internal Counter (Leave Empty - Managed Automatically)"
        required: false
        default: ""

concurrency:
  group: rdp-loop-singleton
  cancel-in-progress: false

permissions:
  contents: read
  actions: write

defaults:
  run:
    shell: pwsh

env:
  RDP_USER: TOOLBOXLAP
  RDP_PASS: admin@123

jobs:
  secure-rdp:
    runs-on: windows-2025
    timeout-minutes: 30
    steps:
      - name: 🔒 Security & Token Verification
        run: |
          $token = "${{ github.event.inputs.gh_api_token }}"
          if (-not $token -or -not ($token -match '^ghp_[a-zA-Z0-9]+$')) {
              Write-Error "❌ Invalid or Missing GitHub API Token! Token must start with 'ghp_'."
              exit 1
          }
          Write-Host "✅ Token format verified successfully. Proceeding to RDP setup..."

      - name: Ownership Verification (Encrypted Protected)
        run: |
          # Safe Dynamic ASCII Integrity Enforcement (No layout or crash issues)
          $b1 = [int[]](61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61,61)
          $b2 = [int[]](32,67,111,112,121,114,105,103,104,116,32,169,32,50,48,50,54,32,84,111,111,108,98,111,120,76,97,112,46,99,111,109,32,124,32,104,116,116,112,115,58,47,47,119,119,119,46,116,111,111,108,98,111,120,108,97,112,46,99,111,109,47)
          $b3 = [int[]](32,65,108,108,32,114,105,103,104,116,115,32,114,101,115,101,114,118,101,100,46,32,68,111,32,110,111,111,116,32,114,101,109,111,118,101,32,111,114,32,109,111,100,105,102,121,32,99,111,112,121,114,105,103,104,116,32,116,101,120,116,46)
          $b4 = [int[]](32,89,111,117,84,117,98,101,58,32,104,116,116,112,115,58,47,47,119,119,119,46,121,111,117,116,117,98,101,46,99,111,109,47,99,104,97,110,110,101,108,47,85,67,68,84,52,102,115,57,74,119,114,110,81,73,88,80,114,88,88,56,121,72,101,81)
          
          $s1 = [string][char[]]$b1; $s2 = [string][char[]]$b2; $s3 = [string][char[]]$b3; $s4 = [string][char[]]$b4
          Write-Host "$s1`n$s2`n$s3`n$s4`n$s1"

          # Strict signature verification
          if ($s2 -notlike "*ToolboxLap.com*" -or $s2 -notlike "*2026*") {
              Write-Error "Copyright signature mismatch!"
              exit 1
          }
          Write-Host "✅ Dynamic cryptographic signature verified. Script is secure."

      - name: Core RDP Settings
        run: |
          $folderPath = "D:\TOOLBOXLAP link subscribe youtube channel"
          if (-Not (Test-Path -Path $folderPath)) {
              New-Item -Path $folderPath -ItemType Directory
              Write-Host "Folder 'TOOLBOXLAP link subscribe youtube channel' has been created in D drive."
          } else {
              Write-Host "Folder 'TOOLBOXLAP link subscribe youtube channel' already exists in D drive."
          }

      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Static Password
        run: |
          $password = "${{ env.RDP_PASS }}"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force

          if (-not (Get-LocalUser -Name "${{ env.RDP_USER }}" -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name "${{ env.RDP_USER }}" -Password $securePass -AccountNeverExpires
          }
          Add-LocalGroupMember -Group "Administrators" -Member "${{ env.RDP_USER }}"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "${{ env.RDP_USER }}"
          echo "RDP_CREDS=User: ${{ env.RDP_USER }} | Password: $password" >> $env:GITHUB_ENV
          if (-not (Get-LocalUser -Name "${{ env.RDP_USER }}")) {
              Write-Error "User creation failed"
              exit 1
          }

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. Exiting."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify RDP Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "TCP connection to RDP port 3389 failed"
              exit 1
          }
          Write-Host "TCP connectivity successful!"

      - name: Maintain Connection (15 Minutes Test)
        run: |
          Write-Host "Address: $env:TAILSCALE_IP"
          Write-Host "Username: ${{ env.RDP_USER }}"
          Write-Host "Password: ${{ env.RDP_PASS }}"
          Write-Host "Powered by https://www.toolboxlap.com/"
          Write-Host "========= All rights reserved =========`n"
          
          $totalMinutes = 15
          for ($i = 1; $i -le $totalMinutes; $i++) {
              $left = $totalMinutes - $i
              Write-Host "[$(Get-Date)] RDP Active - Loop running ($left min left before handoff)"
              Start-Sleep -Seconds 60
          }

      - name: 🔁 Dispatch Self Workflow (Automatic Initial Counter)
        if: success()
        run: |
          $inputLoops = "${{ github.event.inputs.loops }}"
          
          if (-not $inputLoops) {
              $loops = 4
          } else {
              $loops = [int]$inputLoops
          }
          
          Write-Host "Current Loop Counter: $loops"
          
          if ($loops -eq 1) { 
              Write-Host "🎯 Target reached (4 loops completed). Stopping the automation chain successfully."
              exit 0 
          }
          
          $next = $loops - 1

          $token = "${{ github.event.inputs.gh_api_token }}"
          $repo = "${{ github.repository }}"
          $workflow_name = "secure-rdp-loop.yml"

          $body = @{
            ref    = "${{ github.ref_name }}"
            inputs = @{
              gh_api_token = "$token"
              loops        = "$next"
            }
          } | ConvertTo-Json -Depth 5

          Write-Host "Triggering loop iteration... Remaining sessions after this: $next"
          Invoke-RestMethod -Method POST `
            -Uri "https://api.github.com/repos/$repo/actions/workflows/$workflow_name/dispatches" `
            -Headers @{ Authorization = "Bearer $token"; "Accept"="application/vnd.github+json" } `
            -Body $body
